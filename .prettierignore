# =============================================================================
# Prettier ignore list
# =============================================================================
# Files/dirs listed here are excluded from Prettier formatting.
# Lockfiles and build artifacts should not be reformatted.
# =============================================================================

# Build and generated outputs
build
dist
.lovable
dist-*
coverage
node_modules

# Common non-source files
.DS_Store
*.env
*.lock
*.log

# Lockfiles (managed by package manager; do not format)
package-lock.json
bun.lockb

# json-server mock data (structure may be hand-edited)
db.json

# Example assets (no need to format)
public/vite.svg

# Supabase Postgres codegen (`npm run supabase:types:gen`) — avoids massive churn vs Prettier churn on regen
src/integrations/supabase/types.ts

# Long-form docs with intentional layout (tables, lists) — add paths here if Prettier hurts readability
