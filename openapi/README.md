# OpenAPI — Supabase Edge Functions

The file **`supabase-edge-functions.json`** in this directory is **generated**. Do not edit it by hand to change API behavior.

## Regenerate

From the repository root:

```bash
npm run openapi:edge-functions
```

CI-style drift check (regenerates then fails if `git diff` is non-empty):

```bash
npm run openapi:edge-functions:check
```

Per-function detail can be extended via `supabase/functions/<function-name>/openapi.fragment.json`.

**Documentation:** [STANDARDS.md — API artifacts](../docs/STANDARDS.md) · [Documentation index](../docs/README.md).
